import type { Metadata } from "next";
export const metadata: Metadata = {
  title: "neatr.ai — The cleaners booking while you're quoting.",
  description: "The organized operators in your market already run on systems like this. Here's what they know.",
};
export default function V27Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
