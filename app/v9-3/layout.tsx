import type { Metadata } from "next";
export const metadata: Metadata = {
  title: "neaterops — 5-star service. Every job. On autopilot.",
  description: "neaterops turns every booking into a 5-star experience — AI-matched pros, proactive communication, and automated review capture. Build a reputation that sells itself.",
};
export default function V93Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
