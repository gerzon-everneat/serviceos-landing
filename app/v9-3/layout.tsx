import type { Metadata } from "next";
export const metadata: Metadata = {
  title: "neatr.ai — 5-star service. Every job. On autopilot.",
  description: "neatr.ai turns every booking into a 5-star experience — AI-matched pros, proactive communication, and automated review capture. Build a reputation that sells itself.",
};
export default function V93Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
